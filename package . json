{
  "name": "bot-im",
  "version": "1.0.0",
  "main": "artifacts/api-server/dist/index.js",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "cd artifacts/api-server && npx prisma db push && npx tsx dist/index.js",
    "build": "cd artifacts/api-server && npm install"
  },
  "dependencies": {
  }
}
