{
  "name": "bot-im",
  "version": "1.0.0",
  "main": "artifacts/api-server/index.ts",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "install": "cd artifacts/api-server && npm install",
    "build": "cd artifacts/api-server && npx prisma generate",
    "start": "cd artifacts/api-server && npx prisma db push && node --loader tsx index.ts"
  },
  "dependencies": {
    "express": "^4.18.2",
    "tsx": "^4.15.7",
    "telegraf": "^4.16.3",
    "@prisma/client": "^5.0.0"
  }
}
