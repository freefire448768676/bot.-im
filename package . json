{
  "name": "bot-im",
  "version": "1.0.0",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "install": "cd artifacts/api-server && npm install",
    "build": "cd artifacts/api-server && npx prisma generate && npx tsc",
    "start": "cd artifacts/api-server && npx prisma db push && node dist/index.js"
  }
}
